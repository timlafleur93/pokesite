import {Name} from "../Utility/Name";

export class EncounterMethod {
  id: number;
  name: string;
  order: number;
  names: Name[];

  constructor(
    id: number,
    name: string,
    order: number,
    names: Name[],
  ) {

  }
}
